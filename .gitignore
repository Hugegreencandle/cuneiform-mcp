node_modules/
dist/
*.log
.DS_Store
.env
.env.local
.letta/

# private working notes — not for public flip
_private/

# ephemeral agent / tooling dirs — never commit
.claude/
.playwright-mcp/

# generated CuneiBERT training data — never commit
scripts/cuneibert/dataset/

# v0.78.0 image-modality — ProtoSnap sidecar venv + clone + weights are
# UNLICENSED upstream and user-fetched; tablet photos are BM-collection material.
# Cache-only / never committed (the runtime caches live under ~/.cache/cuneiform-mcp/).
scripts/protosnap/.venv/
scripts/protosnap/.cache/
scripts/protosnap/weights/
